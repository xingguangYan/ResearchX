{
  "_comment": "Merge the mcpServers block into your client config. Replace the path with the absolute path of this repository.",
  "mcpServers": {
    "researchx": {
      "command": "python",
      "args": ["/absolute/path/to/ResearchX/mcp-server/researchx_mcp_server.py"],
      "env": {
        "RESEARCHX_MAILTO": "you@institution.edu"
      }
    }
  }
}
